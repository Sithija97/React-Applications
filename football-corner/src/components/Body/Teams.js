import React from 'react'

const Teams = ({name,description}) => {
    return (
        <div>
            <h2>{name}</h2>
            <h4>{description}</h4>
        </div>
    )
}

export default Teams